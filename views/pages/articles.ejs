<%- include('../partials/header', {title : title, session: session}) %>
<div class="container">
   <ul>
        <% articles.forEach((article)=> { %>
            <li>
                <h2>
                    <%= article.title %>
                </h2>
                <p>
                    <%= article.body %>
                </p>
				<p><a href="/article/<%= article.id %>">Detailanzeige</a></p>
            </li>
            <hr />
        <% }) %>
    </ul>
</div>
<%- include('../partials/footer') %>